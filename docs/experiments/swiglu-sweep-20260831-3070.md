# 实验：SwiGLU MLP Block — decode/prefill/train 全 shape 性能 sweep（首次 E3 基线）

- 日期: 2026-08-31
- GPU: NVIDIA GeForce RTX 3070 Laptop GPU (cc 8.6, 8.0 GB)
- 环境: Python 3.12.3 / torch 2.11.0+cu130 / triton 3.6.0 / cuda-tile 1.3.0 / driver 610.88
- 协议: warmup=20, iters=100, CUDA Event; strict FP32 (TF32 关闭); 小 M 自动 64/M 次重复降噪
- 正确性: 每 case 先与 eager FP32 reference(同数据)对比归一化 L2，<1e-4 且有限才进性能汇总
- 完整原始数据: WSL \`artifacts/swiglu_20260831-175309-all-NVIDIA_GeForce_RTX_3070_Laptop_GPU/swiglu_bench.json\` (+ .md 渲染)
- 复现: \`python bench/run.py --suite all --dtypes fp32,fp16 --warmup 20 --iters 100\`

## 汇总（228 cases）

| 项 | 值 |
|---|---|
| cases_total | 228 |
| cases_with_error | 57（fp16 triton/cuda/cutile dtype 边界，见支持矩阵） |
| correctness_failed | 57（全部是 fp16 eager/concat/compile 的参考差异 ~5e-4，见下） |
| 有效性能行 | 171 |
| best speedup | 1.294x（concat fp32 M=16,K=768,F=3072） |
| **任何自定义后端稳定超过 eager** | **否（strict FP32 下）** |

## 后端平均 speedup vs eager（fp32, 19 shapes））

| backend | mean | min | max | 结论 |
|---|---|---|---|---|
| eager | 1.00x | — | — | cuBLAS 基线 |
| triton | 0.838x | 0.479 | **1.176** | 少数 shape 微胜 |
| concat | 0.798x | 0.428 | 1.294 | 单次大 GEMM 大多劣于两独立 GEMM |
| compile | 0.569x | 0.065 | 0.967 | Inductor 3070 上比 eager 慢 |
| cuda | 0.175x | 0.102 | 0.524 | 大 shape 崩坏（最大失败案例） |
| cutile | 0.135x | 0.085 | 0.379 | 最慢 |

## 关键观察（"为什么赢 / 为什么输"）

### Triton 相对赢区（M/K 中等）
- M=2048 × K=768 × F=3072: **1.176x**（全 sweep 第二快）
- M=256/1024 × K=768 × F=3072: 1.10x / 1.05x
- 模式: K=768、M≥256 时 Triton tiled matmul 能接近/略超 cuBLAS FP32；K=4096 大 F 时落后

### 失败案例（最有价值）
1. **cuda/cutile 大 shape 严重落后**: eager M=1024×K=4096×F=11008 = 31.7ms，cuda = 287ms（0.11x）、cutile = 346ms（0.09x）。
   原因假设(待 profile 验证): \`matmul_tiled_auto\` dispatch 在 (M,4096→11008) 这类大 N 上 tile 配置差 / 未走 TensorCore（strict FP32 下 TF32 关）+ 第 3 层 GEMM (F,K) 非连续。
2. **compile 在 3070 上恒慢**（0.065x~0.97x）：Inductor 生成 kernel 未跑赢 cuBLAS，且小 M 时 Python/launch 开销显著。
3. **concat 大多劣于 eager**（0.43~0.80x）：合并成单次 (M, 2F) GEMM 在内存带宽上不划算，仅 decode M=4/16 微胜。

## fp16 现状
- eager/concat/compile: 可跑，norm_l2 ~5e-4（合理）
- triton/cuda/cutile: **全部 dtype 边界报错**（tl.dot 同 dtype / A must be float32 / ct.mma 同 dtype）
- fp16 性能主线（TensorCore）尚未跑通 —— 这是下一个优化阶段的重点

## 下一步（建议）
1. **fp16 支持**: 给 triton matmul 加 input_precision=ieee 或 fp16 输入路径；给 CUDA 加 fp16 matmul（已有 WMMA）
2. **cuda/cutile 大 shape 诊断**: ncu profile 确认 dispatch/tile/tensorcore 瓶颈
3. **decode 小 M 专项**: M=1/4 时 0.1-0.5ms 的差距来自 launch overhead vs kernel 效率，可试 persistent kernel
